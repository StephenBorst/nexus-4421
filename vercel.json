{
  "outputDirectory": "build/client",
  "buildCommand": "node ./node_modules/.bin/tsx scripts/generate-manifest.ts && node ./node_modules/.bin/vite build && node ./node_modules/.bin/tsx scripts/generate-manifest.ts",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}